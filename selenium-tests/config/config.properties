# ===========================================
# CONFIGURATION FOR OTP TESTS
# ===========================================
# For OTP verification tests to work, you MUST use LOCAL backend
# because Railway backend does NOT have test endpoints enabled
# (test endpoints are security risk in production)
#
# TO RUN OTP TESTS:
# 1. Start local backend: cd d:\uniclub\selenium-tests && START_BACKEND_FOR_TESTS.bat
# 2. Use config below with localhost backend
# 3. Run tests: test-otp-only.bat
#
# TO RUN OTHER TESTS (without OTP):
# - Can use Railway backend (OTP tests will auto-skip)
# ===========================================

# LOCAL TESTING (Required for OTP tests)
admin.url=http://localhost:5174
web.url=http://localhost:5173
backend.url=http://localhost:8080

# PRODUCTION TESTING (OTP tests will skip)
# admin.url=https://uniclub-admin.vercel.app
# web.url=https://uniclub-testing.vercel.app
# backend.url=https://uniclubtesting-production.up.railway.app


# Browser Configuration
browser=chrome
headless=false
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# Test Data - Admin User
admin.email=admin@uniclub.vn
admin.password=admin123

# Test Data - Regular User (Production credentials)
user.email=baohuy19112004@gmail.com
user.password=huytran123

# Screenshot Configuration
screenshot.on.failure=true
screenshot.path=test-output/screenshots/

# Reporting
report.path=test-output/reports/
